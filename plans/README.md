# Implementation Plans

Generated by the improve skill on 2026-06-19 and extended on 2026-06-20. Completed plans were pruned after implementation; this index tracks only remaining work.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| [007](007-harden-xlsx-formula-policy.md) | Harden XLSX formula policy beyond the current denylist | P1 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Verification Gates For Every Plan

Run these before marking any plan DONE:

```bash
UV_CACHE_DIR=/tmp/codex-uv-cache uv run python -m pytest -q
UV_CACHE_DIR=/tmp/codex-uv-cache uv run python -m ruff check .
UV_CACHE_DIR=/tmp/codex-uv-cache uv run python -m mypy src tests
```

Expected result: all tests pass, ruff reports `All checks passed!`, and mypy reports `Success: no issues found`.

## Findings Considered And Rejected

- Direction option: encrypted local pseudonym map. Not planned in this batch because the user asked to plan the vetted findings; this is product direction, not a confirmed defect.
- Direction option: workbook feature coverage matrix. Not planned in this batch because it is broader test strategy; it should be considered after Plans 001 and 002 add two more metadata/formula feature checks.
- Direction option: CI setup. Not planned in this batch because it is operational direction, not one of the five vetted code findings.
- Suspected extended document property leak in `docProps/app.xml`: rejected after a tmpdir package probe showed the injected extended properties were not preserved in the sanitized workbook.
